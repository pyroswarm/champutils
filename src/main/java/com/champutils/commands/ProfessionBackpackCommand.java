package com.champutils.commands;

/**
 * Legacy class kept only so old references/imports do not break compilation.
 * The active backpack command is BackpackCommand.
 */
public final class ProfessionBackpackCommand {
    private ProfessionBackpackCommand() {}

    public static void register() {
        // Intentionally empty. Register BackpackCommand and ProfessionTradeCommand instead.
    }
}
